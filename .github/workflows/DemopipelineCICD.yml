name: Demo CICD pipeline

on:
 push: 
  branches:
    - main
 workflow_dispatch:

jobs:
  code:
   runs-on: ubuntu-latest
   steps:
     - name : chechout sourse code 
       run: action/checkout@4

     - name: print message
       run: echo "code clone sussfully"
       
  build:
   runs-on: ubuntu-latest
   needs: code
   
   steps:
     - name : Build the code
       run : echo "This is build the code"
       
  test:
   runs-on: ubuntu-latest
   needs: build

   steps:
     - name: This will test the code
       run: echo "This step is test code"
       
  deploy:
   runs-on: ubuntu-latest
   needs: test

   steps:
     - name : This deploye the code 
       run : echo "This step deploye the code"



       
  
