name: demo
  
on : 
 workflow_dispatch:

jobs :
 code :
  runs-on: ubuntu-latest
  steps:
   - name : Git cloning
     run: echo "clone git code"
 
 build:
  runs-on: ubuntu-latest
  needs: code
  steps:
    - name: build the code
      run : echo "Build the code in docker"


 test:
  runs-on: ubuntu-latest
  needs: build
  steps:
    - name : test the code
      run : echo "Test the code"
  
 deploy:
  runs-on : ubuntu-latest
  needs: test
  steps:
    - name: deploye the code
      run : echo "Deploye the code"
 
       
